SELECT A.JOB_TITLE AS 'Cargo', AVG(B.SALARY) AS 'Média salarial',
CASE
WHEN AVG(B.SALARY) BETWEEN 2000 AND 5800 THEN 'Júnior'
WHEN AVG(B.SALARY) BETWEEN 5801 AND 7500 THEN 'Pleno'
WHEN AVG(B.SALARY) BETWEEN 7501 AND 10500 THEN 'Sênior'
WHEN AVG(B.SALARY) > 10500 THEN 'CEO'
END AS 'Senioridade'
FROM hr.employees AS B 
INNER JOIN hr.jobs AS A
WHERE	 B.JOB_ID = A.JOB_ID
GROUP BY JOB_TITLE
ORDER BY AVG(B.SALARY);
